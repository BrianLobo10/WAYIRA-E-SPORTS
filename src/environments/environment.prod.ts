export const environment = {
  production: true,
  // IMPORTANTE: Cambia esta URL por la URL real de tu servidor backend desplegado
  // Si no tienes backend desplegado, usa temporalmente el localhost (solo para pruebas)
  apiUrl: 'http://localhost:3001/api'
};
